<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="test.openerp">
    e
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".fragments.HomeFragment" />
        <activity android:name=".activities.timesheets.TimeSheetListActivity"></activity>
        <activity android:name=".activities.timesheets.TimesheetsActivity"></activity>
        <activity android:name=".activities.attendance.AttendanceActivity"></activity>
        <activity android:name=".activities.calendar.CalendarActivity"></activity>
        <activity android:name=".activities.leaves.LeaveActivity"></activity>
        <activity android:name=".activities.project.ProjectActivity"></activity>

    </application>

</manifest>